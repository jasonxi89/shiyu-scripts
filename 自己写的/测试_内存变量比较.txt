// 测试内存变量赋值和比较(无空格)
#v = 读内存变量("厨师")
游戏黄字提示("读到:"$#v)
等待("2000")

// 直接赋值后比较
#test = "abc"
如果(#test == "")
  游戏黄字提示("赋值abc比较空:TRUE-异常")
否则()
  游戏黄字提示("赋值abc比较空:FALSE-正常")
如果尾()
等待("2000")

// 内存变量比较空
如果(#v == "")
  游戏黄字提示("内存变量比较空:TRUE")
否则()
  游戏黄字提示("内存变量比较空:FALSE")
如果尾()
等待("2000")

// 手动赋值中文后比较
#v2 = "辉の厨师"
如果(#v2 == "")
  游戏黄字提示("手动中文比较空:TRUE-异常")
否则()
  游戏黄字提示("手动中文比较空:FALSE-正常")
如果尾()
等待("2000")

游戏黄字提示("测试完成")
