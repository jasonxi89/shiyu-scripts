// 药剂师端：检测材料库存，不足100则发布需求单（每单20，最多5单）
循环(""=="")
  写内存变量("药剂师",人物详细信息("1"))

  // 桃木
  如果(获取物品数量("桃木","") < 100)
    写内存变量("桃木_需求1","20")
    写内存变量("桃木_需求2","20")
    写内存变量("桃木_需求3","20")
  如果尾()

  // 番红花
  如果(获取物品数量("番红花","") < 100)
    写内存变量("番红花_需求1","20")
    写内存变量("番红花_需求2","20")
    写内存变量("番红花_需求3","20")
  如果尾()

  // 果梨
  如果(获取物品数量("果梨","") < 100)
    写内存变量("果梨_需求1","20")
    写内存变量("果梨_需求2","20")
    写内存变量("果梨_需求3","20")
  如果尾()

  等待("10000")
循环尾()
