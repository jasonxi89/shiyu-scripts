// 葱采集简略版 - 每20个检查需求+宠邮常开
#DEBUG = "0"

// ============================================================
// 初始化
// ============================================================
#厨师名 = ""

宠邮设置("0")
等待("1000")
宠邮设置("1")
等待("1000")

调用脚本模块("fox逃跑设置")
自动叠加道具("2","葱|20")
自动叠加道具("1","")

// ============================================================
// 主循环: 导航→采集20个→检查需求→补血→继续
// ============================================================
循环("" == "")

  // --- 导航到采集点 ---
  循环(人物详细信息("34") != "库鲁克斯岛")
    如果(人物详细信息("34") == "法兰城")
      调用脚本模块("法兰恢复")
      循环(人物详细信息("34") != "法兰城")
        等待("1000")
      循环尾()
      调用脚本模块("法兰坐船去哥拉尔")
      自动寻路("14","15")
      循环(人物详细信息("34") != "哥拉尔镇")
        等待("1000")
      循环尾()
    如果尾()
    如果(人物详细信息("34") == "哥拉尔镇")
      自动寻路("176","105")
      等待("1000")
    如果尾()
    如果(人物详细信息("46") == "bdk")
      自动寻路("29","26")
      护士恢复("2")
      等待("2000")
      自动寻路("9","23")
      循环(人物详细信息("34") != "哥拉尔镇")
        等待("1000")
      循环尾()
      自动寻路("176","105")
      等待("1000")
    如果尾()
    如果(人物详细信息("34") != "库鲁克斯岛" && 人物详细信息("34") != "哥拉尔镇" && 人物详细信息("34") != "法兰城" && 人物详细信息("46") != "bdk")
      登出回城()
      等待("2000")
    如果尾()
    等待("1000")
  循环尾()

  自动寻路("287","429")
  等待("1000")

  // --- 采集20个(阻塞) ---
  高速采集("狩猎","5500","1","","","葱","20")
  如果(#DEBUG == "1")
    游戏黄字提示("[D]葱="$获取物品数量("葱","")$",MP="$人物详细信息("4"))
  如果尾()

  // --- 读需求+控制宠邮 ---
  #需求字符串 = 读内存变量("材料需求")
  分割文本(#需求数组,#需求字符串,"|","")

  如果(#需求数组.1 != "" && #需求数组.1 != "0" && #需求数组.1 > "0")
    // 有需求:读厨师名+配置宠邮
    如果(#厨师名 == "")
      #厨师名 = 读内存变量("厨师")
    如果尾()
    如果(#厨师名 != "")
      #宠邮配置 = "葱|20||"$#厨师名
      调用脚本模块("fox辅助设置")
      如果(#DEBUG == "1")
        游戏黄字提示("[D]有需求,宠邮配置给"$#厨师名)
      如果尾()
    如果尾()
  否则()
    // 无需求:清空厨师名停止发送
    如果(#厨师名 != "")
      #厨师名 = ""
      #宠邮配置 = "|||"
      调用脚本模块("fox辅助设置")
      如果(#DEBUG == "1")
        游戏黄字提示("[D]无需求,清空厨师名")
      如果尾()
    如果尾()
  如果尾()

  // --- MP耗尽→补血 ---
  如果(人物详细信息("4") <= "0")
    如果(#DEBUG == "1")
      游戏黄字提示("[D]MP耗尽,补血")
    如果尾()
    自动寻路("283","437")
    循环(人物详细信息("34") != "哥拉尔镇")
      等待("1000")
    循环尾()
    自动寻路("165","91")
    循环(人物详细信息("46") != "bdk")
      等待("1000")
    循环尾()
    自动寻路("29","26")
    护士恢复("2")
    等待("2000")
    自动寻路("9","23")
    循环(人物详细信息("34") != "哥拉尔镇")
      等待("1000")
    循环尾()
  如果尾()

循环尾()
